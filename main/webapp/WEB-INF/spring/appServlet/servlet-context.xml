<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->

	<!-- 
		어노테이션 기반의 Spring MVC를 활성화합니다.
		이로 인해 요청 처리, 데이터 바인딩, 유효성 검사 등을 리졸브하는 핸들러 맵핑 및 핸들러 어댑터 구성이 활성화됩니다.
	-->
	<annotation-driven />

	<!-- 
		HTTP GET 요청을 처리하여 정적 리소스를 ${webappRoot}/assets 디렉토리에서 제공합니다.
	-->
	<!-- 정적 파일 경로 매핑 -->
	<resources mapping="/assets/**" location="/assets/" />

	<!-- 
		InternalResourceViewResolver를 사용해 @Controllers에서 반환된 뷰를 /WEB-INF/views 디렉토리 안의 .jsp 리소스로 변환합니다.
	(앞에 /view/로 경로 설정해주고 뒤에 .jsp붙게 해주니까 뷰 리졸버로 보낼때 그냥 jsp 이름만 쓰면 됨)-->
	<!-- 뷰 리졸버 - 컨트롤러에서 돌아오는 뷰의 경로를 전체 경로로 변경한다. -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>

	<!-- 
		Base package 경로를 설정하여 스프링 런타임이 해당 패키지와 그 하위 패키지에서 @Controller 및 기타 애너테이션을 포함한 컴포넌트를 찾아 빈으로 등록합니다.
	-->
	<context:component-scan base-package="com.multicampus.Shoes4Jo" />
	
</beans:beans>
